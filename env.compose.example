# Used by docker-compose.prod.yml for ${POSTGRES_PASSWORD} substitution.
# Copy this to .env.compose on the VPS and set a real password.
# This file is NOT the app's runtime env — see .env.prod.example for that.

POSTGRES_PASSWORD=set-a-real-password-here
